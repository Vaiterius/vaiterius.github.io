const SectionSubheader = ({ text }) => {
	return (
		<h3 className="text-2xl font-black">
			{text} <span className="font-light text-accent">//</span>
		</h3>
	);
};

export default SectionSubheader;
